# Dependabot keeps two surfaces fresh:
#
#  - github-actions: our workflows pin actions to commit SHAs (supply-chain
#    posture — a moved tag can't swap the code under us). Dependabot is the
#    rotation mechanism: it PRs the new SHA + version comment together, so
#    the pins stay current without hand-resolving tags.
#
#  - npm: runtime deps are exact-pinned (no ranges) and kept deliberately
#    few; Dependabot PRs version bumps so upgrades are explicit, reviewed
#    diffs rather than silent range drift. The install-scripts CI guard and
#    lockfile-diff workflow both run on these PRs.
version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    commit-message:
      prefix: "chore(actions)"
  - package-ecosystem: npm
    directory: /
    schedule:
      interval: weekly
    commit-message:
      prefix: "chore(deps)"
    groups:
      dev-dependencies:
        dependency-type: development
    ignore:
      # @types/node majors track Node majors. Stay on the types matching the
      # minimum supported runtime (engines.node >=22) — newer types would let
      # code reference APIs that don't exist where sigil actually runs.
      - dependency-name: "@types/node"
        update-types: ["version-update:semver-major"]
      # TypeScript majors are gated on typescript-eslint's peer range
      # (currently <6.1). TS 7 (the native port) also ships per-platform
      # binary packages, which is a supply-chain surface we don't want to
      # take on by accident. Bump deliberately, not via the weekly group.
      - dependency-name: "typescript"
        update-types: ["version-update:semver-major"]
